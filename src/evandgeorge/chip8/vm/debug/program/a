                        ''/

Node** childPointer = null;

while(childPointer == null || *childPointer != null) {
	if(v > curnode->val)
		childPointer = &curnode->right;
	else if(v < curnode->val
		childPointer = &curnode->left;

	curnode = *childPointer;
};

*childPointer = new Node(v);